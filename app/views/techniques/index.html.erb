<div class="event" id="events">
	<div class="container">
		<div class="top-event">
			<h3>石材工法</h3>
		</div>
		<%= @techniques.each_slice(4) do |te4| %>
			<% te4.each_with_index do |te, i| %>
				<div class="<%= i.even? ? 'event-grid' : '' %>">
					<div class="wow <%= render_wrapper_direction(i) %> animated" data-wow-delay=".5s">
						<%= link_to te do %>
							<%= image_tag (te.attachments.cover.try(:first).try(:image).try(:url) || "default.svg"), class: "img-responsive" %>
						<% end %>
						<div class="<%= render_content_direction(i) %>">
							<div class="event-gr">
								<h4><%= link_to te.full_name, te %></h4>
								<p><%= te.description.try(:truncate, 100) %></p>
							</div>
						</div>
					</div>
					<% if i.odd? %>
						<div class="clearfix"></div>
					<% end %>
				</div>
			<% end %>
		<% end %>
		<div class="clearfix"></div>
	</div>
</div>